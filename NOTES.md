# Follow-ups

Items to revisit once upstream fixes land. Grouped by trigger.

## When SPEL issue #140 is resolved

[logos-co/spel#140](https://github.com/logos-co/spel/issues/140) tracks the stale scaffold default and the `ruint@1.18 / rustc 1.88` clash.

- Drop `cargo update -p ruint --precise 1.17.0 --manifest-path methods/guest/Cargo.toml` from `scripts/demo.sh` and the README's build section.
- Stop passing `--spel-rev refs/heads/main` to `spel init`; default should just work.

## When SPEL issue #141 is resolved

[logos-co/spel#141](https://github.com/logos-co/spel/issues/141) tracks the divergence between `generate_idl!` proc macro and `spel generate-idl` CLI around `#[account_type]` markers.

- Revert the `Makefile` `idl` target back to `cargo run --bin generate_idl > $(IDL_FILE)` (the proc-macro path), which is cheaper than invoking the CLI.
- The scaffolded `examples/src/bin/generate_idl.rs` will then pick up `#[account_type]` correctly and we can drop the CLI call.

## When SPEL re-exports `nssa::program_methods::AUTHENTICATED_TRANSFER_ID`

Today `spel_framework_core/src/lib.rs:20-21` only re-exports from `nssa_core`, not `nssa`. The `AUTHENTICATED_TRANSFER_ID` constant is generated by `nssa/build.rs` and lives in `nssa::program_methods`. Rather than pull `nssa` as a fresh dep (adds `risc0-build` as a build dep for one constant), we **hardcoded** the `[u32; 8]` value in `methods/guest/src/bin/whisper_wall.rs` from a cached `nssa` build output for LEZ tag `v0.2.0-rc1` (commit `35d8df0`).

- **If LEZ version is bumped**, regenerate the constant from the new `program_methods/mod.rs` (any successful `cargo build` of `nssa` will produce one at `target/*/build/nssa-*/out/program_methods/mod.rs`).
- **If SPEL adds a re-export** (e.g. `pub use nssa::program_methods::AUTHENTICATED_TRANSFER_ID` in the prelude), delete the hardcoded constant and use the import.

## When we care about a proper test suite

`methods/guest/src/bin/whisper_wall.rs` has no unit tests. The modern pattern (see `/home/vpavlin/devel/github.com/logos-co/spel/tests/e2e/fixture_program/src/lib.rs`) uses `#[cfg(test)]` handler calls directly — no zkVM, no proof generation. Worth adding:
- `initialize_sets_admin`
- `whisper_on_nonempty_wall_errors`
- `overwrite_rejects_equal_or_lower_tip`
- `drain_jar_only_admin`
- `__claims_overwrite` / `__validate_overwrite` shape checks for the ChainedCall plumbing

## When we want a web UI

A thin JS client over `spel`'s JSON output (`--dry-run=json`, `spel inspect --type WhisperState`) would make the "anonymous bidding war" demo much more compelling. The `spel-client-gen` crate produces typed FFI bindings — could generate a WASM client too.

## `--bin-<name>` is required for any chained-call target under private TX

Private TX proof generation in `nssa::privacy_preserving_transaction::circuit::execute_and_prove` looks up each emitted `ChainedCall.program_id` in a `dependencies` map built from `ProgramWithDependencies`. If a ChainedCall's target isn't in that map, the builder returns `NssaError::InvalidProgramBehavior` (see `nssa/src/privacy_preserving_transaction/circuit.rs:118` at LEZ `v0.2.0-rc1` / 35d8df0).

`spel-cli/src/tx.rs:352-361` builds the dependency map from the `--bin-<NAME> <FILE>` flags. For `overwrite` (which fires a ChainedCall to `auth-transfer`), we therefore need:

```bash
spel --bin-auth-transfer /path/to/artifacts/program_methods/authenticated_transfer.bin \
  -- overwrite --signer Private/… --msg … --tip N
```

Without the `--bin-auth-transfer` flag, a `Public/` signer still succeeds (the public path doesn't build a per-TX dependency map the same way), but the private path panics at `wallet/src/lib.rs:402` with `InvalidProgramBehavior`. The same flag works for `Public/` too — it's just silently ignored on the public path.

The auth-transfer binary ships inside `nssa`'s build artifacts:

```
~/.cargo/git/checkouts/logos-execution-zone-<hash>/<rev>/artifacts/program_methods/authenticated_transfer.bin
```

Nicer UX follow-up: `spel` could auto-discover well-known chained targets (`auth-transfer`, `token`, `amm`, `ata`) from the IDL or from a `spel.toml` `[chained_deps]` section, so users don't need to know where the binary lives. Currently the user must pass the explicit file path.

## PDA-as-sender ChainedCall pattern (deferred)

The original plan had `drain_jar` emit a `ChainedCall` to `auth-transfer` with the wall PDA as sender, using `Option<PdaSeed>` to prove PDA authority. This does not work out of the box for our setup: `auth-transfer`'s `transfer()` handler only checks `sender.is_authorized`, but the LEZ runtime then enforces "only the owning program can decrease the sender's balance" — and our wall is owned by whisper-program, not auth-transfer. The ChainedCall would be rejected at the runtime layer.

Instead, `drain_jar` does the balance move directly in its own post-state (the program owns the wall → it's allowed to decrement; increments to the recipient are always permitted). Simpler, correct, and avoids the cross-program ownership dance entirely.

The PDA-as-sender + `PdaSeed` pattern remains the right shape when a PDA must pay a program it doesn't own — e.g. a vault managed by program A paying into a token program B. Out of scope for this demo, and would need an end-to-end runtime test of what the pda_seeds proof is actually checked against. The `/home/vpavlin/devel/github.com/logos-co/lez-multisig/multisig_program/src/execute.rs:115-155` path uses this pattern but the multisig PDA is the recipient in that ChainedCall flow, not the sender.

## Known scope that was cut

- `clear_wall` (admin-only no-payment reset). Would trivially reset `latest_whisper` to `""` and `last_tip` to 0. Skipped because `drain_jar` already covers the "admin override" gesture.
- Multi-slot wall (last N whispers). The account data byte budget would need review — currently we rely on `String` being bounded by whatever the runtime permits.
- Tip leaderboard PDA tracking top tippers. Interesting with private accounts (commitment-only leaderboard), but out of scope for this demo.
